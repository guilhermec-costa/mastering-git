git restore <file> # restore content of the file in the working tree
git restore --staged <file> # moves file from staging area to working tree
git restore --staged --worktree # restore staging area and working tree
git restore --source=HEAD~1 # restore a file from 1 commit before the HEAD commit 