git status  # shows untracked files, modified files in the working tree, and files staged for commit
git status --untracked-files=[no|normal|all]  # controls which untracked files are shown
git status --porcelain  # stable, script-friendly output
